function Level(name, map, background, normalGravity, enemies) {
    this.name = name;
    this.map = map;
    this.background = background;
    this.normalGravity = normalGravity;
    this.enemies = enemies;
}